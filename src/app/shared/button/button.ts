import { Component, Input } from '@angular/core';
@Component({
  selector: 'button[appButton]',
  standalone: true,
  templateUrl: './button.html',
  styleUrl: './button.css',
  imports: [],
})
export class Button {}
